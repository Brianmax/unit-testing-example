spring.application.name=demo

spring.datasource.url=jdbc:postgresql://localhost:5432/pricingdem   o
spring.datasource.username=postgres
spring.datasource.password=admin

spring.jpa.hibernate.ddl-auto=validate
spring.jpa.open-in-view=false

spring.liquibase.change-log=classpath:db/changelog/db.changelog-master.xml
# Loads db/changelog/changes/900-seed-demo-data.sql.
# Remove this line (or set it to an empty value) before pointing this app at a real database.
spring.liquibase.contexts=demo-data

spring.jackson.deserialization.fail-on-unknown-properties=true

app.invoice.tax-rate=0.075
